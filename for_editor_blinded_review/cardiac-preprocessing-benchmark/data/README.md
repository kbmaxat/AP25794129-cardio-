# Data policy

This package includes no real patient data.

For blinded review:
- use synthetic data (`scripts/create_synthetic_sample.py`), or
- run with publicly available datasets stored outside repository root.

Do not commit identifiable clinical data.
